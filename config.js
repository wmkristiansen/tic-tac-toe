// Configuration for the game
const config = {
    serverUrl: window.location.hostname === 'localhost' ? 'http://localhost:3000' : 'https://your-server-url.herokuapp.com' // Replace with your deployed server URL
};

export default config;